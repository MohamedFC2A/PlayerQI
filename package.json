{
  "name": "player-qi",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "release": "node server/scripts/generate-changelog.js",
    "start": "node server/index.js",
    "dev": "npm run dev --prefix client & npm run dev --prefix server"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "axios": "^1.13.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "openai": "^4.28.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
